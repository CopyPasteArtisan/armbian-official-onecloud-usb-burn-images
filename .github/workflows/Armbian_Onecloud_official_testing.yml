name: Armbian Onecloud official Testing

on:
  workflow_dispatch:
    inputs:
      image_url:
        description: 'Download URL of Armbian img.xz'
        required: true
        default: 'https://armbian.lv.auroradev.org/dl/onecloud/archive/Armbian_24.11.1_Onecloud_bookworm_current_6.6.43_minimal.img.xz'

env:
  SCRIPT_REPO: ${{ github.repository }}
  SCRIPT_REF: ${{ github.ref_name }}
  UBOOT_REPO: hzyitc/u-boot
  UBOOT_RELEASE: latest
  UBOOT_BURNIMG: eMMC.burn.img
  UBOOT_TAG: build-20221028-0940

permissions:
  contents: write

jobs:
  build:
    name: Build burn.img
    runs-on: ubuntu-22.04

    steps:
      - name: Install Dependents
        run: |
          TIME="$(curl https://api.github.com/repos/${SCRIPT_REPO}/actions/runs/${GITHUB_RUN_ID} | jq -r .created_at)"
          TAG="CI-$(date -d "${TIME}" -u +'%Y%m%d-%H%M%S-%Z')"
          echo "TAG=$TAG" >> $GITHUB_ENV
          
          sudo apt-get update
          sudo apt-get install img2simg

          ver="v0.3.1"
          echo "https://github.com/hzyitc/AmlImg/releases/download/$ver/AmlImg_${ver}_linux_amd64"
          curl -L -o ./AmlImg https://github.com/hzyitc/AmlImg/releases/download/$ver/AmlImg_${ver}_linux_amd64
          chmod +x ./AmlImg

      - name: Download and decompress Official Armbian Image
        run: |
          set -euo pipefail
          echo "::group::下载镜像..."
          IMAGE_URL="${{ github.event.inputs.image_url }}"
          echo "下载链接: $IMAGE_URL"

          # download image
          curl -L -O "$IMAGE_URL"
          imgfile=$(basename "$IMAGE_URL")
          echo "downloaded: $imgfile"

          # try to download accompanying .sha (sha256) file and verify
          sha_url="$IMAGE_URL.sha"
          echo "尝试下载 SHA 文件: $sha_url"
          if curl -fsSL -o image.sha "$sha_url"; then
            echo "SHA 文件下载成功: image.sha"
            expected_hash=$(awk '{print $1}' image.sha)
            echo "expected: $expected_hash"
            actual_hash=$(sha256sum "$imgfile" | awk '{print $1}')
            echo "actual:   $actual_hash"
            if [ "$expected_hash" != "$actual_hash" ]; then
              echo "ERROR: sha256 mismatch for $imgfile"
              cat image.sha
              exit 1
            else
              echo "SHA256 verified OK"
            fi
          else
            echo "Warning: could not download $sha_url — skipping remote sha verification"
          fi

          ls -la
          echo "::endgroup::"
          
          echo "::group::解压镜像..."
          xz -d *.img.xz

          mkdir -p output/images
          mv *.img output/images
          ls -la output/images/*.img
          echo "::endgroup::"

      - name: Download and unpack the latest u-boot
        run: |
          echo "::group::Download"
          echo "https://github.com/${UBOOT_REPO}/releases/download/${UBOOT_TAG}/${UBOOT_BURNIMG}"
          curl -L -o ./uboot.img https://github.com/${UBOOT_REPO}/releases/download/${UBOOT_TAG}/${UBOOT_BURNIMG}
          echo "::endgroup::"

          echo "::group::Unpack"
          mkdir -p burn
          ./AmlImg unpack ./uboot.img burn/
          echo "::endgroup::"

      - name: Extract boot and rootfs partitions
        run: |
          diskimg=$(find output/images -name "*.img" | head -n 1)
          echo "disk image: $diskimg"
          loop=$(sudo losetup --find --show --partscan $diskimg)
          sudo img2simg ${loop}p1 burn/boot.simg
          sudo img2simg ${loop}p2 burn/rootfs.simg
          sudo losetup -d $loop
          sudo chown $(id -u):$(id -g) -R burn/

      - name: Generate burn image
        run: |
          echo -n "sha1sum $(sha1sum burn/boot.simg | awk '{print $1}')" >burn/boot.VERIFY
          echo -n "sha1sum $(sha1sum burn/rootfs.simg | awk '{print $1}')" >burn/rootfs.VERIFY

          cat <<EOF >>burn/commands.txt
          PARTITION:boot:sparse:boot.simg
          VERIFY:boot:normal:boot.VERIFY
          PARTITION:rootfs:sparse:rootfs.simg
          VERIFY:rootfs:normal:rootfs.VERIFY
          EOF

          prefix=$(find output/images -name "*.img" | head -n 1 | sed 's/\.img$//')
          burnimg=${prefix}.burn.img
          ./AmlImg pack $burnimg burn/
          
          for f in output/images/*.img; do
              case "$f" in
                  *.burn.img) continue ;;
                  *) rm -f "$f" ;;
              esac
          done

          ls -la output/images

      - name: Hash and compress images
        run: |
          set -euo pipefail
          # compute sha256 and compress each .img -> .img.xz
          for f in output/images/*.img; do
            sha256sum "$f" | tee -a sha256sum
            xz --threads=0 --compress "$f"
          done
          
          image_name=$(ls output/images | head -n 1)
          echo "IMAGENAME=$image_name" >> $GITHUB_ENV

          # download remote description (.img.txt) derived from IMAGE_URL
          IMAGE_URL="${{ github.event.inputs.image_url }}"
          if [[ "$IMAGE_URL" =~ \.img\.xz$ ]]; then
            TXT_URL="${IMAGE_URL%.img.xz}.img.txt"
          else
            TXT_URL="$IMAGE_URL.txt"
          fi
          echo "尝试下载描述文件: $TXT_URL"
          mkdir -p output/images
          if curl -fsSL -o output/images/${image_name}.txt "$TXT_URL"; then
            echo "描述文件下载成功: output/images/${image_name}.txt"
          else
            echo "Warning: 描述文件不存在或无法下载: $TXT_URL"
          fi

          # create a tar.xz bundle containing all .img.xz and the text description(s)
          cd output/images
          shopt -s nullglob
          imgs=( *.img.xz )
          if [ ${#imgs[@]} -gt 0 ]; then
            base="${image_name%.img.xz}"
            tar -cJf "${base}.tar.xz" "${imgs[@]}" *.txt || true
            echo "Created bundle: ${base}.tar.xz"
          else
            echo "No .img.xz files found to bundle"
          fi
          cd - >/dev/null || true

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: ${{ env.IMAGENAME }}_${{ env.TAG }}
          path: output/images/*

      - name: Generate release informations
        run: |
          cat <<EOF | sed -E 's/^  //' | tee Release.md
            $(cat sha256sum | awk '{printf "%s: `%s`\n", $2, $1}')
          EOF

